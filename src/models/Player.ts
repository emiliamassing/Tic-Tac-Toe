export default class Player {
    constructor(public name: string, public shape: string, public isPlaying: boolean){
        this.name = name;
        this.shape = shape;
        this.isPlaying = isPlaying;
    };
};