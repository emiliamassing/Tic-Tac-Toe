export default class Player {
    constructor(public name: string, public shape: string, public wins: number){
        this.name = name;
        this.shape = shape;
        this.wins = wins;
    };
};